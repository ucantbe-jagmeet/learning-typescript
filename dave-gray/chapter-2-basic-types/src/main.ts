let myName: string = 'Jagmeet'
let meaningOfLife: number 
let isLoading: string | boolean
let album: string | number

myName = 'John'
meaningOfLife = 42
// isLoading = true
isLoading = "true"
album = 1984

const sum = ( a:number, b:number) => {
    return a+b
}

let postId: string | number
let isActive: boolean | number

let re : RegExp = /\w+/g